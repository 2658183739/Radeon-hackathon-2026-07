# ROCm-enabled PyTorch must be installed from the approved Radeon environment.
# The evidence runtime was Python 3.12, ROCm 7.2.1, and torch 2.9.1+rocm7.2.1.
# No generic PyPI torch pin is used here because it could install a non-ROCm build.
# Core runtime used by the checkpoint-free scripted Genesis demo.
# Docker installs the pinned Genesis source before resolving this list.
numpy>=1.26
pyarrow>=16
pillow>=10
matplotlib>=3.11,<3.12
imageio>=2.34
av>=12
opencv-python-headless>=4.9
genesis-world==1.2.3

# Optional learned-policy dependency: install with `pip install .[lerobot]`.
# It is not required by scripts/run_demo.sh.
